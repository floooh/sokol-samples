fips_begin_lib(ozzanim)
    fips_dir(src)
    fips_files(ozz_base.cc ozz_animation.cc mesh.cc)
fips_end_lib()
target_include_directories(ozzanim PUBLIC include)
if (FIPS_CLANG OR FIPS_GCC)
    target_compile_options(ozzanim PUBLIC -Wno-missing-field-initializers $<$<COMPILE_LANGUAGE:CXX>:-Wno-deprecated-copy>)
endif()
if (FIPS_GCC)
    target_compile_options(ozzanim PUBLIC -Wno-ignored-attributes)
endif()
if (FIPS_MSVC)
    target_compile_definitions(ozzanim PRIVATE _CRT_SECURE_NO_WARNINGS)
endif()
