fips_begin_lib(stb)
    fips_files(stb_image.c stb_image.h)
    if (FIPS_CLANG OR FIPS_GCC)
        target_compile_options(stb PRIVATE -Wno-sign-conversion -Wno-unused-function)
    endif()
fips_end_lib(stb)
