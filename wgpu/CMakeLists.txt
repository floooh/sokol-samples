fips_begin_lib(wgpu_entry)
    fips_files(wgpu_entry.c wgpu_entry.h)
    if (FIPS_EMSCRIPTEN)
        fips_files(wgpu_entry_wasm.c)
    elseif (FIPS_OSX)
        fips_files(wgpu_entry_osx.mm)
        fips_libs(dawnutil)
        fips_frameworks_osx(Cocoa QuartzCore Metal MetalKit)
    endif()
fips_end_lib()

fips_begin_app(clear-wgpu windowed)
    fips_files(clear-wgpu.c)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(triangle-wgpu windowed)
    fips_files(triangle-wgpu.c)
    sokol_shader(triangle-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(quad-wgpu windowed)
    fips_files(quad-wgpu.c)
    sokol_shader(quad-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(bufferoffsets-wgpu windowed)
    fips_files(bufferoffsets-wgpu.c)
    sokol_shader(bufferoffsets-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(cube-wgpu windowed)
    fips_files(cube-wgpu.c)
    sokol_shader(cube-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(noninterleaved-wgpu windowed)
    fips_files(noninterleaved-wgpu.c)
    sokol_shader(noninterleaved-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(texcube-wgpu windowed)
    fips_files(texcube-wgpu.c)
    sokol_shader(texcube-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(offscreen-wgpu windowed)
    fips_files(offscreen-wgpu.c)
    sokol_shader(offscreen-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(instancing-wgpu windowed)
    fips_files(instancing-wgpu.c)
    sokol_shader(instancing-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(mrt-wgpu windowed)
    fips_files(mrt-wgpu.c)
    sokol_shader(mrt-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(arraytex-wgpu windowed)
    fips_files(arraytex-wgpu.c)
    sokol_shader(arraytex-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(dyntex-wgpu windowed)
    fips_files(dyntex-wgpu.c)
    sokol_shader(dyntex-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(uvwrap-wgpu windowed)
    fips_files(uvwrap-wgpu.c)
    sokol_shader(uvwrap-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(mipmap-wgpu windowed)
    fips_files(mipmap-wgpu.c)
    sokol_shader(mipmap-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(blend-wgpu windowed)
    fips_files(blend-wgpu.c)
    sokol_shader(blend-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(sdf-wgpu windowed)
    fips_files(sdf-wgpu.c)
    sokol_shader(sdf-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(imgui-wgpu windowed)
    fips_files(imgui-wgpu.cc)
    fips_deps(wgpu_entry imgui)
fips_end_app()

fips_begin_app(inject-wgpu windowed)
    fips_files(inject-wgpu.c)
    sokol_shader(inject-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(sgl-lines-wgpu windowed)
    fips_files(sgl-lines-wgpu.c)
    fips_deps(wgpu_entry)
fips_end_app()

fips_begin_app(plmpeg-wgpu windowed)
    fips_files(plmpeg-wgpu.c)
    sokol_shader(plmpeg-wgpu.glsl wgpu)
    fips_dir(../sapp/data)
    fipsutil_copy(plmpeg-assets.yml)
    fips_deps(wgpu_entry)
    if (FIPS_OSX)
        fips_frameworks_osx(AudioToolbox)
    endif()
fips_end_app()

fips_begin_app(cubemaprt-wgpu windowed)
    fips_files(cubemaprt-wgpu.c)
    sokol_shader(cubemaprt-wgpu.glsl wgpu)
    fips_deps(wgpu_entry)
fips_end_app()
